"""
SnapBlock block loading — stub.

Will hold the runtime block loader: append a single block by type_id from the
bundled snapblock_library.blend (bpy.data.libraries.load, link=False) and link
it into the "SnapBlock Build" collection at a grid-snapped location.

Not wired up yet — added in the "add block" step.
"""
